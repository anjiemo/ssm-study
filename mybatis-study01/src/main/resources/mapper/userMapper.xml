<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.cqautotest.mapper.UserMapper">
    <select id="selectUserById" resultType="cn.cqautotest.entity.User">
        select *
        from user
        where id = #{id}
    </select>

    <select id="findAllUsers" resultType="cn.cqautotest.entity.User">
        select *
        from user
    </select>

    <select id="queryUsers" resultType="cn.cqautotest.entity.User">
        select id, username, password
        from user
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userName !=null and userName != ''">
                and username = #{userName}
            </if>
            <if test="password !=null and password != ''">
                and password = #{password}
            </if>
        </where>
    </select>

    <select id="findUsersByName" resultType="cn.cqautotest.entity.User">
        select *
        from user
        where username like '%${userName}%'
    </select>

    <insert id="saveUser" parameterType="cn.cqautotest.entity.User">
        insert into user (id, username, password)
        values (#{id}, #{userName}, #{password})
    </insert>

    <update id="updateUser" parameterType="cn.cqautotest.entity.User">
        update user
        set username = #{userName},
            password = #{password}
        where id = #{id}
    </update>

    <delete id="deleteUserById" parameterType="cn.cqautotest.entity.User">
        delete
        user from user where id =
        #{id}
    </delete>
</mapper>